{
  "name": "hw2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "backend": "cd backend && yarn dev",
    "frontend": "cd frontend && yarn dev",
    "install": "cd frontend && yarn install && cd ../backend && yarn install",
    "format": "cd frontend && yarn format && cd ../backend && yarn format"
  }
}
